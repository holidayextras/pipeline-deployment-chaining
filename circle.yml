machine:
  python:
    version: 2.7.10

dependencies:
  pre:
    - pip install -r requirements.txt --upgrade
    - python $HOME/$CIRCLE_PROJECT_REPONAME/circlestatus/check_circle_status.py google.com

test:
  override:
    - pep8 *.py
    - python -m unittest discover -s circlestatus/test
