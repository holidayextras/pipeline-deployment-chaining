machine:
  python:
    version: 2.7.10

dependencies:
  pre:
    - pip install -r requirements.txt --upgrade
    - python $HOME/$CIRCLE_PROJECT_REPONAME/circlestatus/check_circle_status.py a_link

test:
  override:
    - pep8 *.py
    - python -m unittest discover -s circle_status/test
