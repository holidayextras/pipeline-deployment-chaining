machine:
  python:
    version: 2.7.10

dependencies:
  pre:
    - pip install -r requirements.txt --upgrade
    - python $HOME/$CIRCLE_PROJECT_REPONAME/circlestatus/check_circle_status.py https://circleci.com/api/v1/project/holidayextras/pipeline-controller-staging/tree/master?circle-token=56a35b1ee0d69515b3f4a22a9a25a82a9a144185

test:
  override:
    - pep8 *.py
    - python -m unittest discover -s circlestatus/test
